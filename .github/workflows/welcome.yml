name: Post welcome comment

on:
  pull_request:
    types: [opened]

permissions:
  pull_requests: write

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "job1"